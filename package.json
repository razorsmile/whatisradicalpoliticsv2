{
  "name": "radical-education",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "hugo server",
    "build": "hugo --minify",
    "clean": "node scripts/clean.js",
    "package": "node scripts/package.js",
    "dev": "npm run clean && npm run build && npm run package && hugo server"
  }
}
